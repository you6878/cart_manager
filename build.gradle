plugins {
    id 'maven-publish'
}

publishing {
    publications {
        release(MavenPublication) {
            groupId = 'com.github.you6878'
            artifactId = 'cart_manager'
            version = '1.0.2'

            artifact("$projectDir/libs/manager.aar")
        }
    }
}